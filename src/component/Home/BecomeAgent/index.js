import React from 'react';
import { Wrapper } from './style';

const BecomeAgent = () => {
  return (
    <Wrapper>
      <Wrapper.Title>Become a Real Estate Agent</Wrapper.Title>
      <Wrapper.Description>
        We only work with the best companies around the globe
      </Wrapper.Description>
      <Wrapper.Button>REGISTER NOW</Wrapper.Button>
    </Wrapper>
  );
};

export default BecomeAgent;
